# Build Caddy with the Namecheap DNS plugin
FROM caddy:2-builder AS builder
RUN xcaddy build --with github.com/caddy-dns/namecheap v2.9.1

FROM caddy:2
COPY --from=builder /usr/bin/caddy /usr/bin/caddy
